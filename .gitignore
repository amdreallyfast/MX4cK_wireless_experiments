# I only want to track C# files and text files, so ignore everything, then 
# selectively un-ignore certain extensions, then extend that un-ignoring to all
# subfolders.

*
!*.cs
!*.txt
#!/VS_2013_CS_wireless_access_point/
#!/VS_2013_CS_sockets_and_MX4cK/

# expand these rules to directory deep to reach the root folder of each project
!/*/